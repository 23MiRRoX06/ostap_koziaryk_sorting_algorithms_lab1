name: ostap_koziaryk_sorting_algorithms_lab1
description: Command-line application for first lab. Written on Dart. Uses quickSort and insertionSort to sort vacation packages by price and duration
# version: 1.0.0
# homepage: https://www.example.com
environment:
  sdk: '>=2.8.1 <3.0.0'

#dependencies:
#  path: ^1.7.0
dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4

dependencies:
  string_validator: ^0.1.4
